"""Running the question stream against a context layer, and scoring what came
back. Kept apart from `contextlayer/` on purpose: a scorer that shares code
with the thing it scores agrees with it for the wrong reasons."""
