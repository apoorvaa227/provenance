"""Adapters that map a real tool's metadata into the catalog shape.

The generator proves the layer works on a graph designed for it, which is a
weaker claim than it sounds. These prove the context model survives contact
with a format someone else defined.
"""
